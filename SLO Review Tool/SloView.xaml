<Window x:Class="SloReviewTool.SloView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SloReviewTool"
        mc:Ignorable="d"
        Title="SloView" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5,5,5,5" />
        </Style>
        <Style x:Key="Value" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5,0,5,0" />
        </Style>
        <Style x:Key="Label" TargetType="TextBlock" BasedOn="{StaticResource Value}">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0">
            <TextBlock Text="Service Name: " Style="{StaticResource Label}" />
            <TextBlock Text="{Binding ServiceName}" Style="{StaticResource Value}" />
            <TextBlock Text="Service Group Name: " Style="{StaticResource Label}" />
            <TextBlock Text="{Binding ServiceGroupName}" Style="{StaticResource Value}"/>
            <TextBlock Text="Service Id: " Style="{StaticResource Label}" />
            <TextBlock Text="{Binding ServiceId}" Style="{StaticResource Value}"/>
        </WrapPanel>
        <TextBlock Text="Data Sources:" Grid.Row="1" Style="{StaticResource Header}" />
        <ListView x:Name="DataSourcesListView" Grid.Row="2" MinHeight="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="Id: " Style="{StaticResource Label}" />
                        <TextBlock Text="{Binding Id}" Style="{StaticResource Value}" />
                        <TextBlock Text="Type: " Style="{StaticResource Label}" />
                        <TextBlock Text="{Binding Type}" Style="{StaticResource Value}"/>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Text="SLO Groups:" Grid.Row="3" Style="{StaticResource Header}" />
        <ListView x:Name="SloGroupssListView" Grid.Row="4" MinHeight="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="Name: " Style="{StaticResource Label}" />
                        <TextBlock Text="{Binding Name}" Style="{StaticResource Value}" />
                        <TextBlock Text="State: " Style="{StaticResource Label}" />
                        <TextBlock Text="{Binding State}" Style="{StaticResource Value}"/>
                        <ListView ItemsSource="{Binding Slos}">
                            <TextBlock Text="Name: " Style="{StaticResource Label}" />
                            <TextBlock Text="{Binding Name}" Style="{StaticResource Value}" />
                        </ListView>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Text="SLO Definition:" Grid.Row="5" Style="{StaticResource Header}" />
        <TextBox x:Name="YamlText" Text="{Binding SloDefinition.SloText, Mode=OneWay}" Grid.Row="6" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
