<Window x:Class="SloReviewTool.SloView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SloReviewTool"
        mc:Ignorable="d"
        Title="SloView" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5,5,5,5" />
        </Style>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5,0,5,0" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding ServiceName, Mode=OneTime, StringFormat=Service Name:              {0}}" Grid.Row="0" Style="{StaticResource Label}" />
        <TextBlock Text="{Binding ServiceGroupName, Mode=OneTime, StringFormat=Service Group Name:  {0}}" Grid.Row="1" Style="{StaticResource Label}" />
        <TextBlock Text="{Binding ServiceId, Mode=OneTime, StringFormat=Service Id:                    {0}}" Grid.Row="2" Style="{StaticResource Label}" />
        <TextBlock Text="SLO Definition:" Grid.Row="3" Style="{StaticResource Header}" />
        <TextBox x:Name="YamlText" Text="{Binding SloDefinition.SloText, Mode=OneWay}" Grid.Row="4" IsReadOnly="True"/>
    </Grid>
</Window>
